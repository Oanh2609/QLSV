@model IEnumerable<QLSV.Models.MonHoc>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admins/Views/Shared/_LayoutADM.cshtml";
}

<h2>Môn học</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenMonHoc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SoTin)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenMonHoc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SoTin)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaMonHoc }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaMonHoc }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaMonHoc })
        </td>
    </tr>
}

</table>
